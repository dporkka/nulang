// bootstrap/self_test.nula — Minimal Nulang Core test program
//
// The bootstrap emitter targets this program: `fn main() { 42 }`.
// Stage 1 verifies the emitter can produce valid .nbc bytecode for it.
// Stage 2 will compile this program through the full bootstrap pipeline.

fn main() {
    42
}
