// Algebraic effects — a handler intercepts a performed effect and
// resumes the computation with a value.
//
// Run with: nulang examples/effects.nula

handle perform Math.getAnswer() with {
    Math.getAnswer() => 42
}
