// generics_12 — tuple payload splat now works: Make(1, 2) correctly
// packs the two arguments into a single tuple payload, matching the
// typechecker's splat convention. Previously the VM rejected this.
type Pair[T] = Make((T, T))

fn main() {
    let p = Make(1, 2)
    perform IO.print("ok")
}
