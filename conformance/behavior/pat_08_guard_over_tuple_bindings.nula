fn compare(pair) -> String {
    match pair {
        | (a, b) if a > b => "gt"
        | (a, b) if a < b => "lt"
        | (a, b) => "eq"
    }
}
perform IO.print(compare((3, 1)) + "," + compare((1, 3)) + "," + compare((2, 2)))
