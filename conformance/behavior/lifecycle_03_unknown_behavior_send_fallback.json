{"exit_code": 0, "stdout": "1", "proves": "FINDING (undefined behavior dispatch): sending a message for a behavior name the target does not declare compiles without error and does NOT drop the message -- it executes the actor's FIRST declared behavior (index 0). The compiled Send carries behavior_id 2 (module table has 2 entries; 'nosuch' resolves past the end), and the runtime's bytecode_offsets.get(idx).unwrap_or(0) falls back to offset 0 = bump's code, so n goes 0 -> 1. The runtime comment at the dispatch site says unknown ids 'should be skipped, but be defensive'; the actual behavior is a silent fallback to behavior 0."}
