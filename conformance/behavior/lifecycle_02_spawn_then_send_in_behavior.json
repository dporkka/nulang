{"exit_code": 0, "stdout": "7", "proves": "A behavior spawns a child actor and immediately sends it a message in the same step (make(): spawn Child {} then send ch put(7) -- not two separate top-level statements). The child's Address round-trips through parent state (state child: Address), and asking the child's get() behavior -- which flushes the queued put(7) first -- returns 7: the spawn-then-send side effect took effect exactly once."}
