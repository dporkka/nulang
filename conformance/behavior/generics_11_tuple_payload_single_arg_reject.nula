// generics_11 — tuple payload now accepts both splatted form Make(1, 2)
// AND tupled form Make((1, 2)). The arity check allows a single tuple
// argument when the function expects a tuple parameter.
type Pair[T] = Make((T, T))

fn main() {
    let p = Make((1, 2))
    perform IO.print("ok")
}
